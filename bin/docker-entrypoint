#!/bin/bash -e

# Run migrations if starting the web server (via Thruster or directly)
if [[ "$*" == *"./bin/rails server"* ]] || [[ "$*" == *"bin/rails server"* ]]; then
  ./bin/rails db:prepare
fi

exec "${@}"
