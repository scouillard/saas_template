Reset your password

Hi <%= @resource.email %>,

Someone has requested a link to change your password. You can do this by visiting the link below:

<%= edit_user_password_url(reset_password_token: @token) %>

If you didn't request this, you can safely ignore this email. Your password won't change until you access the link above and create a new one.
