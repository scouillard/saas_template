<div class="max-w-4xl mx-auto px-4 py-8">
  <h1 class="text-2xl font-semibold text-base-content mb-6"><%= @project.name %></h1>

  <div class="space-y-4">
    <% @tasks.each do |task| %>
      <%= render "tasks/task", task: task %>
    <% end %>
  </div>
</div>
